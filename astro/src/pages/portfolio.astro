---
import { getCollection } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import PortfolioSection from '../components/PortfolioSection.astro';

const entries = await getCollection('portfolioEn');
---
<Layout title="Portfolio — leins275" description="Education, open source, talks, certifications." lang="en" activeNav="portfolio">
  <PortfolioSection lang="en" entries={entries} />
</Layout>
